# Hello, world!

Code snippet:

<CodeSnippet lang="node" snippet="
const foo = 3;

const bar = (props) => {
  if (foo === 4) {
    return null;
  }
}

console.log('hii');" />

<CodeSnippet lang="python" snippet="print('hello world')"/>

<CodeSnippet lang="python" snippet="print('hello\" world')"/>

## note section

Note:

<Note>Yeah baby</Note>

Multi note:

<Note>
multi line

note multi line note multi line note
</Note>

## inline note section

Note:

<Note text="Yeah baby" />

Multi note:

<Note text="
multi line

note multi line note multi line note
" />
